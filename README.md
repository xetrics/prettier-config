# `@xetrics/prettier-config`

> My personal [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add --dev @xetrics/prettier-config
```

**Edit `package.json`**:

```jsonc
{
	// ...
	"prettier": "@xetrics/prettier-config"
}
```
